1. Dynamtic Table - sap ui Table make in controller, not in view (use ui.table)
2. use this https://ui5.sap.com/#/entity/sap.ui.table.Table/sample/sap.ui.table.sample.Basic
DONE. 

CRUD on table records. 
Add C, R, U, D buttons. 

Add will contain 2 cases 
If table is empty then also you have to add.
If not empty 
Then replicate the last row and add to 1st place and then user can edit its details and save it. 
use Dialog to tell the user if to add a new one or add a old one. 

And save will contain 2 cases.. edit and add
 
Abhijit Kumar
And save will contain 2 cases.. edit and add
With same button
 
For edit The selected item will come into enable mode inline editable. 
 
And for delete..the selected row will be deleted. 
 
